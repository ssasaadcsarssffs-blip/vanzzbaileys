import type { AuthenticationState } from "./storage"

export async function initAuthState(): Promise<AuthenticationState> {
  throw new Error(
    "initAuthState() has not been implemented yet."
  )
}
