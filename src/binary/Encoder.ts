/**
 * Binary Encoder
 */

import type { BinaryNode } from "./BinaryNode"

export function encodeBinaryNode(
    node: BinaryNode
): Uint8Array {

    throw new Error(
        "encodeBinaryNode() not implemented."
    )

}
